<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="Rika\SyliusBrandPlugin\Entity\BrandTranslation" table="rika_brand_translation">
        <id name="id" type="integer" column="id">
            <generator strategy="AUTO" />
        </id>

        <field name="name" type="string" column="name" length="255" nullable="false" />
        <field name="slug" type="string" column="slug" length="255" nullable="false" />
        <field name="description" type="text" column="description" nullable="true" />
        <field name="locale" type="string" column="locale" length="8" nullable="false" />

        <!-- Relation avec Brand -->
        <many-to-one field="translatable" target-entity="Rika\SyliusBrandPlugin\Entity\Brand" inversed-by="translations">
            <join-column name="translatable_id" referenced-column-name="id" on-delete="CASCADE" nullable="false" />
        </many-to-one>

        <!-- Index composé pour l'unicité -->
        <unique-constraints>
            <unique-constraint name="UNIQ_BRAND_TRANSLATION_LOCALE" columns="translatable_id,locale" />
            <unique-constraint name="UNIQ_BRAND_SLUG_LOCALE" columns="slug,locale" />
        </unique-constraints>

        <!-- Index pour améliorer les performances -->
        <indexes>
            <index name="IDX_BRAND_TRANSLATION_LOCALE" columns="locale"/>
        </indexes>
    </entity>

</doctrine-mapping>
